# DBLift Runtime Dependencies (Minimal for Docker images)
# Use this for production Docker images instead of requirements.txt
# IMPORTANT: Keep versions in sync with pyproject.toml [project].dependencies

# Core dependencies
PyYAML>=6.0.1

# CLI and formatting
rich>=13.3.0
Jinja2>=3.1.0

# SQL validation (can be optional for migration-only images)
# NOTE: Version must match pyproject.toml (>=30.0.0)
sqlglot>=30.0.0,<31

# Native driver integration
SQLAlchemy>=2.0.0

# No database drivers here, by design: they live in the pyproject extras
# (`dblift[postgresql]`, `dblift[cosmosdb]`, … or `dblift[all]`). An image that
# needs to reach a database installs the extra on top of this file.
